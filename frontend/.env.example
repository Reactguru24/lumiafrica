# Backend API URL
# Local backend:  http://localhost:8080
# Deployed backend (Railway): use the URL below — npm run dev proxies requests to avoid CORS
NEXT_PUBLIC_API_URL=https://dazzling-smile-production-1014.up.railway.app

# AI virtual fitting service (server-side proxy only — set when ML is deployed)
# ML_API_URL=https://your-ml-service.onrender.com
# NEXT_PUBLIC_VIRTUAL_FITTING_ENABLED=true
