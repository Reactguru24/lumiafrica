# Host-specific URLs and Railway variable refs: deploy/railway.env.example
# Redeploy trigger: 2026-07-04 (AI fitting, delivery zones, unified docker)

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/app/lumi-backend"
healthcheckPath = "/health"
